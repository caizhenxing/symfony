<!DOCTYPE html>
<html>
    <head>
        <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
        <link rel="stylesheet" href="{{ asset('bundles/walkthrough/css/global.css') }}" type="text/css" media="all" />
        <link rel="stylesheet" href="{{ asset('bundles/walkthrough/css/bootstrap.css') }}" type="text/css" />
   		 {% stylesheets '@ParkThroughBundle/Resources/css/application.css' filter='cssrewrite' %}
       		 <link rel="stylesheet" href="{{ asset_url }}" />
 		   {% endstylesheets %}
        <title>{% block title %}SEGA_NW{% endblock %}</title>
        <link rel="shortcut icon" href="{{ asset('favicon.ico') }}" />
    </head>
    <body>
            {% if app.session.flash('notice') %}
                <div class="flash-message">
                    {{ app.session.flash('notice') }}
                </div>
            {% endif %}

            {% block content_header %}
                <div style="clear: both"></div>
            {% endblock %}

        {% block content %}
        {% endblock %}

        <br/><br/><br/><br/>
        {% block footer %}
        {% endblock %}

    </body>
</html>
